---
interface Props {
  text: string;
  author: string;
}

const { text, author } = Astro.props;
---
<section class="py-20 bg-gray-50">
  <div class="max-w-4xl mx-auto px-4 sm:px-6 lg:px-8 text-center">
    <blockquote class="text-2xl md:text-3xl font-display italic text-gray-800">
      "{text}"
    </blockquote>
    <cite class="mt-4 block text-lg text-gray-600">— {author}</cite>
  </div>
</section>